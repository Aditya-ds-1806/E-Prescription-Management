<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team 1948</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/navbar.css">
</head>

<body>
    <% if(user.role === 'doctor' || user.role === 'patient') { %>
    <%- include('./partials/navbar', {user: user}) %>
    <% } %>
    <div class="container">
        <h1 class="text-center m-3">My Prescriptions</h1>
        <% if(!user.prescriptions.length) { %><h5 class="text-danger text-center">Nothing to show :(</h5><% } %>
        <ul class="list-group m-5">
            <% for(let i = 0; i < user.prescriptions.length; i++) { %>
            <a href=<%= `/prescription/${user.prescriptions[i]}` %>>
                <li class="list-group-item"><%= user.prescriptions[i] %></li>
            </a>
            <% } %>
        </ul>
    </div>


</body>

</html>