<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Team 1948</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/index.css">
</head>

<body>
    <header class="container-lg text-white text-center">
        <div class="row justify-content-center align-items-center">
            <div class="col-12">
                <h1 class="display-2 text-uppercase"><strong>E-Prescription Management System</strong></h1>
            </div>
            <% if(loggedIn) { %>
            <img class="rounded-circle img-fluid" src="<%= doctor.image %>" alt="Profile picture">
            <div class="col-12">
                <h1 class="display-4 text-capitalize">Dr. <%= doctor.name.toLowerCase() %></h1>
            </div>
            <% } %>
        </div>

        <% if(!loggedIn) { %>
        <div class="row justify-content-around align-items-center">
            <div class="col-10 bg-info rounded my-5">
                <h3 class="p-2 mb-0">Please sign in to generate new prescription</h3>
            </div>
            <div class="col-8 col-sm-4">
                <a href="/auth/google" class="btn btn-light btn-lg mx-auto d-block shadow-lg">Sign in with Google</a>
            </div>
        </div>
        <% } %>

        <% if(loggedIn && doctor.hasUpdatedDetails) { %>
        <div class="row justify-content-around align-items-center mt-5">
            <div class="col-6 col-md-4">
                <a href="/prescription" class="btn btn-light btn-lg mx-auto d-block">New Prescription</a>
            </div>
            <div class="col-6 col-md-4">
                <a href="" class="btn btn-light btn-lg mx-auto d-block">Verify Authenticity</a>
            </div>
        </div>
        <% } %>

        <% if(loggedIn) { %>
        <div class="row justify-content-around align-items-center mt-3">
            <div class="col-6 col-md-4">
                <a href="/profile" class="btn btn-light btn-lg mx-auto d-block">Update Profile</a>
            </div>
            <div class="col-6 col-md-4">
                <a href="/auth/logout" class="btn btn-light btn-lg mx-auto d-block">Logout</a>
            </div>
        </div>
        <% } %>
        </div>
    </header>
</body>

</html>